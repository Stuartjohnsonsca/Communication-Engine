{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run prisma:deploy && npm start",
    "healthcheckPath": "/api/health",
    "//healthcheckTimeout": "300s covers cold-start with all 74 migrations + seed; first-time deploy on a fresh Postgres takes 60-120s. Previous value 30s caused Network>Healthcheck failures on cold start even though Build + Deploy + migrations all completed cleanly.",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "numReplicas": 1
  }
}
